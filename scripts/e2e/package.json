{
  "name": "jellyfin-data-flow-e2e",
  "private": true,
  "description": "Headless-browser end-to-end check of the Data Flow panel against the dev Jellyfin container.",
  "scripts": {
    "install-browsers": "npx playwright install chromium && npx playwright install chrome",
    "e2e": "node e2e.js",
    "e2e:hls": "FORCE_TRANSCODE=1 THROTTLE_BPS=500000 THROTTLE_SECONDS=100 node e2e.js"
  },
  "devDependencies": {
    "playwright": "1.55.0"
  }
}
